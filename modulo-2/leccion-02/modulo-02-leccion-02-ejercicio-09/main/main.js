'use strict'

const name = 'Alexandra López';

const printName = document.querySelector('.name');

printName.innerHTML = `Mi nombre es ${name} y está compuesto por ${name.length} caracteres.`;

